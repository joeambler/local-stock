export default class Shop {
    id: any;
    name: String;
    address: String;

    constructor (id: any, name: String, address: String){
        this.id = id;
        this.name = name;
        this.address = address;
    }
}